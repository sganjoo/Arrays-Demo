pipeline {
  agent {
    node {
      label 'Test'
    }

  }
  stages {
    stage('Setup') {
      agent {
        node {
          label 'Test'
        }

      }
      steps {
        echo 'running setup'
      }
    }
  }
}